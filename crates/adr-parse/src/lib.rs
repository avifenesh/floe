//! TypeScript parsing → `adr-core` graph. Scope 1 day 2+: tree-sitter ingest, file
//! → function/type nodes, basic call edges. scip-typescript integration lands when
//! the tree-sitter-only path shows its recall gap.

pub fn placeholder() {}
